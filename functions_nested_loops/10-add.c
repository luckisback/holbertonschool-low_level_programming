#include "main.h"

/**
 * add - function that adds two integers and returns the result.
 * @x: the first argument
 * @y: seconde argument
 * Return: provids the result of maths
 */

int add(int x, int y)
{
	int result;

	result = (x + y);
	return (result);
}
