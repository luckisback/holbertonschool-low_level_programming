#include "main.h"

/**
* _abs - Function that computes the absolute value of an integer.
* @i: Farater that print the value of variable
* Return: retuns the absolut value of "i"
*/

int _abs(int i)
{
	if (i < 0)
		i *= -1;
	return (i);
}

