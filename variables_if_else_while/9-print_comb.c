#include <stdio.h>

/**
  * main - Entry point
  * Return: Alwayse 0 (Success)
  */

int main(void)
{
	int i;
	i = 48;

	for (i < 57; i++)
		putchar(i);
		putchar(44);
		putchar(32);
	while ( i == 57)
		putchar(36);
	return (0);
}
