#include "main.h"

/**
* puts2 - function that prints every other character of a string
* @str: argument
*
*/

void puts2(char *str)
{
	char var[];
	int i;
	int z;

	while (i[str] != '\0')
		var[i] = str;
		_putchar(var[i]);
		i i = 2;
}
