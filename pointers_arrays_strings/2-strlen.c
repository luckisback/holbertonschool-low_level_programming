#include "main.h"

/**
 * _strlen -  function that returns the length of a string
 *
 * @s: argument (pointer)
 * Return: provid the result of the function
 */

int _strlen(char *s)
{
	int Nbr_add = 0;

	while (Nbr_add[s] != '\0')
		Nbr_add++;
	return (Nbr_add);
}
