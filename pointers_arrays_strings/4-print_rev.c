#include "main.h"

/**
 * print_rev - function that prints a string, in reverse
 * @s: argument
 *
 */

void print_rev(char *s)
{
	int i = 0;
	int r;

	while (i[s] != '\0')
	{
		i++;
	}
	for (r = i; r--;)
		_putchar(r[s]);
	_putchar('\n');
}
